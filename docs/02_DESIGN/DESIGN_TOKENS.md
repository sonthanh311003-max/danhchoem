# DIGITAL MEMORIES
# PHASE 8.5
# DESIGN TOKENS

Version 1.0

---

# PURPOSE

This document defines the visual foundation variables of Digital Memories.
Every color, spacing, radius, shadow, and motion transition must reference these tokens.

---

# TOKENS SPECIFICATION

* **Colors**:
  * Surface: `surface-primary` (ivory), `surface-secondary` (paper), `surface-tertiary` (cream)
  * Accents: `accent-love` (dusty rose), `accent-nostalgia` (burgundy), `accent-gold` (gold)
* **Spacing**: Bội số của 8px (`space-1` = 4px, `space-2` = 8px, `space-3` = 12px, `space-4` = 16px, `space-6` = 24px, `space-8` = 32px, `space-12` = 48px).
* **Border Radius**: `radius-xs` (2px), `radius-sm` (4px), `radius-md` (8px), `radius-lg` (12px), `radius-xl` (18px).
* **Shadows**: Soft multi-layered shadows representing natural light (`shadow-soft`, `shadow-paper`, `shadow-premium`).
* **Motion**: `duration-fast` (150-250ms), `duration-normal` (250-450ms), `duration-cinematic` (600-1200ms).
